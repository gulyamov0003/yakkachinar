import type { NavKey } from './translations';

/** Navigation order matches the page order. Sections join a group through `data-nav`. */
export const NAV_ITEMS: readonly NavKey[] = ['overview', 'experience', 'menu', 'gallery', 'events', 'reviews', 'location'];
